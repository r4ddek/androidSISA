<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sisa.ufrpe.br.sisaandroid">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sisa_brand"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/SisaStyle">
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadastroActivity"
            android:configChanges="orientation|keyboardHidden|screenSize" />
        <activity
            android:name=".Cadastro2Activity"
            android:configChanges="orientation|keyboardHidden|screenSize" />
        <activity
            android:name=".ModificacaoActivity"
            android:configChanges="orientation|keyboardHidden|screenSize" />

        <!-- interno -->

        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/InternoStyle" />
        <activity
            android:name=".VerSugestoesActivity"
            android:label="@string/title_activity_ver_sugestoes"
            android:theme="@style/InternoStyle" />
        <activity
            android:name=".FeedbackActivity"
            android:label="@string/title_activity_feedback"
            android:theme="@style/InternoStyle" />
        <activity
            android:name=".SugestaoActivity"
            android:label="@string/title_activity_sugestao"
            android:theme="@style/InternoStyle" />
        <activity
            android:name=".GradeActivity"
            android:label="@string/title_activity_grade"
            android:theme="@style/InternoStyle" />
        <activity android:name=".DetalheSugestaoActivity"></activity>
    </application>

</manifest>